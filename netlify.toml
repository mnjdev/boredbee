[build]
command = "npm run build"
publish = "out"

[build.environment]
NEXT_PUBLIC_SITE_URL = "https://boredbee.fun"
NEXT_PUBLIC_ADSENSE_ENABLED = "false"

# BoredBee is static-first, so Netlify serves the exported `out` directory.
