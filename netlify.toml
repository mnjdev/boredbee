[build]
command = "npm run build"
publish = ".next"

[build.environment]
NEXT_PUBLIC_SITE_URL = "https://boredbee.fun"
NEXT_PUBLIC_ADSENSE_ENABLED = "false"

# Netlify automatically detects Next.js and uses the @netlify/next runtime.
